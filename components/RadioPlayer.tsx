import { Pause, Play, Shuffle, SkipBack, SkipForward } from "lucide-react";
import type { Track } from "@/data/vibes";
const fmt = (s:number) => `${Math.floor(s/60)}:${Math.floor(s%60).toString().padStart(2,"0")}`;
export default function RadioPlayer({ track, playing, currentTime, duration, onPlayPause, onNext, onPrevious, onShuffle, onSeek }: { track: Track; playing:boolean; currentTime:number; duration:number; onPlayPause:()=>void; onNext:()=>void; onPrevious:()=>void; onShuffle:()=>void; onSeek:(n:number)=>void }) { const pct = duration ? currentTime/duration*100 : 0; return <div className="player-enter relative z-30 mx-auto mb-5 mt-auto w-[calc(100%-2rem)] max-w-3xl rounded-[2rem] border border-cream/20 bg-[linear-gradient(110deg,rgba(96,67,48,.9),rgba(132,59,43,.9))] p-3 shadow-[0_24px_70px_rgba(0,0,0,.45)] backdrop-blur-2xl sm:mb-8 sm:rounded-full sm:p-3.5"><div className="flex items-center gap-3 sm:gap-5"><div className="relative h-16 w-16 shrink-0 overflow-hidden rounded-full bg-amber/20 ring-1 ring-cream/30 sm:h-20 sm:w-20">{track.youtubeId ? <img src={`https://i.ytimg.com/vi/${track.youtubeId}/hqdefault.jpg`} alt="" className={`h-full w-full object-cover ${playing?"animate-[spin_18s_linear_infinite]":""}`}/> : <span className="grid h-full w-full place-items-center font-display text-lg text-cream/60">FM</span>}<span className="absolute left-1/2 top-1/2 h-3 w-3 -translate-x-1/2 -translate-y-1/2 rounded-full bg-[#6c4435] ring-2 ring-cream/60"/></div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h3 className="truncate text-sm font-semibold sm:text-base">{track.title}</h3><p className="mt-0.5 truncate text-xs text-cream/60 sm:text-sm">{track.artist}</p></div><button aria-label="Zufälligen Titel spielen" onClick={onShuffle} className="hidden text-cream/55 transition hover:text-cream sm:block"><Shuffle size={15}/></button></div><button aria-label="Position im Titel" onClick={(e) => { const r=e.currentTarget.getBoundingClientRect(); onSeek(((e.clientX-r.left)/r.width)*duration) }} className="relative mt-3 block h-3 w-full"><span className="absolute left-0 right-0 top-1/2 h-1 -translate-y-1/2 rounded-full bg-cream/25"/><span className="absolute left-0 top-1/2 h-1 -translate-y-1/2 rounded-full bg-cream" style={{width:`${pct}%`}}/><span className="absolute top-1/2 h-2 w-2 -translate-y-1/2 rounded-full bg-cream" style={{left:`calc(${pct}% - 4px)`}}/></button><div className="flex justify-between text-[10px] tabular-nums text-cream/55"><span>{fmt(currentTime)}</span><span>{fmt(duration)}</span></div></div><div className="flex shrink-0 items-center gap-1 sm:gap-2"><button aria-label="Vorheriger Titel" onClick={onPrevious} className="control h-8 w-8"><SkipBack size={16} fill="currentColor"/></button><button aria-label={playing?"Pause":"Abspielen"} onClick={onPlayPause} className="grid h-12 w-12 place-items-center rounded-full bg-cream text-ink shadow-lg transition hover:scale-105 sm:h-14 sm:w-14">{playing?<Pause size={19} fill="currentColor"/>:<Play className="ml-0.5" size={19} fill="currentColor"/>}</button><button aria-label="Nächster Titel" onClick={onNext} className="control h-8 w-8"><SkipForward size={16} fill="currentColor"/></button></div></div></div> }
