import { Dice5, X } from "lucide-react";
import type { Vibe } from "@/data/vibes";
export default function MoodSelector({ vibes, onSelect, onClose }: { vibes: Vibe[]; onSelect: (v: Vibe) => void; onClose: () => void }) { return <div className="fixed inset-0 z-50 overflow-y-auto bg-[#0d0b09]/95 p-5 backdrop-blur-xl sm:p-10" role="dialog" aria-modal="true" aria-label="Stimmung wählen"><button onClick={onClose} aria-label="Schließen" className="fixed right-5 top-5 z-10 grid h-11 w-11 place-items-center rounded-full border border-cream/20 bg-ink/70 hover:text-amber"><X/></button><div className="mx-auto flex min-h-full max-w-6xl flex-col justify-center py-12"><div className="text-center"><p className="text-[10px] font-bold uppercase tracking-[.3em] text-amber">Dein Abend. Deine Zeit.</p><h2 className="mt-3 font-display text-4xl font-bold uppercase sm:text-6xl">Wie fühlst du dich heute?</h2><p className="mt-3 text-sm text-cream/50">Wähle eine Stimmung. Wir kümmern uns um den Rest.</p></div><div className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{vibes.map((v, i) => <button key={v.id} onClick={() => onSelect(v)} className="group relative min-h-64 overflow-hidden border border-cream/15 text-left transition hover:-translate-y-1 hover:border-amber"><img src={v.background} alt="" className="absolute inset-0 h-full w-full object-cover transition duration-700 group-hover:scale-105"/><span className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-transparent"/><span className="absolute left-5 top-5 font-display text-xs text-cream/45">0{i+1}</span><span className="absolute bottom-5 left-5 right-5"><b className="block font-display text-2xl uppercase tracking-wide">{v.title}</b><span className="mt-1 block text-xs text-cream/60">{v.cardSubtitle}</span></span></button>)}</div><button onClick={() => onSelect(vibes[Math.floor(Math.random()*vibes.length)])} className="mx-auto mt-8 flex items-center gap-2 border-b border-cream/30 pb-1 text-xs font-bold tracking-[.2em] hover:border-amber hover:text-amber"><Dice5 size={15}/> ZUFALL</button></div></div> }
