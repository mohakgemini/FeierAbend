import { Radio } from "lucide-react";
import type { Vibe } from "@/data/vibes";
export default function VibeBadge({ vibe, onChange, listeners, time }: { vibe: Vibe; onChange: () => void; listeners:number; time:string }) { return <header className="relative z-20 p-5 sm:p-8 lg:p-10"><div className="grid grid-cols-3 items-center text-xs text-cream/75 sm:text-sm"><time>{time}</time><div className="flex items-center justify-self-center gap-2 whitespace-nowrap"><span className="h-2 w-2 animate-pulse rounded-full bg-emerald-400"/><span>{listeners} online</span></div><button onClick={onChange} className="justify-self-end border-b border-cream/30 pb-1 text-[9px] font-bold tracking-[.14em] transition hover:border-amber hover:text-amber sm:text-[10px]">STIMMUNG</button></div><div className="mt-7 flex items-center gap-3"><span className="grid h-8 w-8 place-items-center rounded-full border border-amber/40 bg-ink/25 text-amber backdrop-blur"><Radio size={14}/></span><div><p className="font-display text-lg font-bold uppercase tracking-wide">{vibe.title}</p><p className="text-[10px] text-cream/50">{vibe.subtitle}</p></div></div></header> }
